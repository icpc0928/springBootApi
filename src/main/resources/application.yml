server:
  port: 8888

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
    active: dev

#debug: true

#級別等級 trace < info < warn < error
#設定日誌級別   root:info 全部的log會顯示info級別以上的 ,也可個別針對套件/路徑 做個別等級
#logging:
#  level:
#    root: info
#    org.springframework.web: warn
#    com.leo: debug
#  file:
#    name: logs/debug.log
#logging:
#  config: classpath:logback-spring.xml
